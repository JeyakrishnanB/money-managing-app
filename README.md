# Getting Started with Money Managing App

This project was built with [React](https://reactjs.org/) and [Vite](https://vitejs.dev/) using **pnpm** as the package manager.

## Available Scripts

In the project directory, you can run:

### `pnpm dev`

Runs the app in the development mode.
Open [http://localhost:5173](http://localhost:5173) to view it in your browser.

The page will reload when you make changes.
You may also see any lint errors in the console.

### `pnpm build`

Builds the app for production to the `dist` folder.
It correctly bundles React in production mode and optimizes the build for the best performance.

The build is minified and the filenames include the hashes.
Your app is ready to be deployed!

### `pnpm preview`

Previews the production build locally.
Open [http://localhost:4173](http://localhost:4173) to view it in your browser.

### `pnpm lint`

Runs ESLint to check for code errors and warnings in the project.

## Project Structure

```
moneymanagingapp/
├── public/          # Static assets
├── src/             # Source code
├── .gitignore
├── eslint.config.mjs
├── package.json
├── pnpm-lock.yaml
├── pnpm-workspace.yaml
└── README.md
```

## Installation

Clone the repository and install dependencies:

```bash
git clone https://github.com/JeyakrishnanB/money-managing-app.git
cd money-managing-app
pnpm install
pnpm dev
```

## Learn More

To learn React, check out the [React documentation](https://reactjs.org/).

To learn Vite, check out the [Vite documentation](https://vitejs.dev/).
